rootProject.name = 'heroku-console-demo'

for (File file: rootDir.listFiles()) {
    if (file.isDirectory() &&
            file.name.startsWith("org.jgrapes.osgi.") ) {
            include file.name
    }
}

include 'cnf'
