enableFeaturePreview('STABLE_PUBLISHING')

rootProject.name = 'heroku-portal-demo'

for (File file: rootDir.listFiles()) {
    if (file.isDirectory() &&
            file.name.startsWith("org.jgrapes.osgi.") ) {
            include file.name
    }
}

include 'cnf'
